name: "🚀 BarkCloud.Web CI/CD"

on:
  push:
    branches:
      - dev
      - master
    paths:
      - 'Backend/BarkCloud.Web/**'
      - 'Shared/**'
      - 'Tests/Backend/BarkCloud.Web.Tests/**'
      - 'Tests/BarkCloud.TestKit/**'
      - 'Backend/rebuild.trigger'
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  ci:
    uses: ./.github/workflows/backend-service-ci.yml
    with:
      service-name: BarkCloud.Web
      service-key: web
      service-path: Backend/BarkCloud.Web
      test-path: Tests/Backend/BarkCloud.Web.Tests
      project-path: Backend/BarkCloud.Web/BarkCloud.Web.csproj
      test-project-path: Tests/Backend/BarkCloud.Web.Tests/BarkCloud.Web.Tests.csproj
      dockerfile: Backend/BarkCloud.Web/Dockerfile.slim
      image-name: barkcloud-web
      web-client-directory: Backend/BarkCloud.Web/ClientApp
      force-run: ${{ github.event_name == 'workflow_dispatch' }}
    secrets: inherit
