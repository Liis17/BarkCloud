FROM mcr.microsoft.com/dotnet/aspnet:10.0-noble
WORKDIR /app
RUN apt-get update \
    && apt-get install -y --no-install-recommends libgssapi-krb5-2 \
    && rm -rf /var/lib/apt/lists/*
# Статические бинари ffmpeg/ffprobe для генерации превью видео и аудио-обложек.
COPY --from=mwader/static-ffmpeg:latest /ffmpeg /usr/local/bin/ffmpeg
COPY --from=mwader/static-ffmpeg:latest /ffprobe /usr/local/bin/ffprobe
COPY --chown=1654:1654 publish/runtimes/ ./runtimes/
COPY --chown=1654:1654 publish/logs/ ./logs/
COPY --chown=1654:1654 publish/*.dll publish/*.json publish/*.pdb publish/*.config ./
USER $APP_UID
ENTRYPOINT ["dotnet", "BarkCloud.Files.dll"]
